fastapi==0.111.0
uvicorn[standard]==0.30.1
influxdb-client==1.43.0
httpx==0.27.0
python-dotenv==1.0.1
python-multipart==0.0.9
growattServer>=1.5.0
openpyxl==3.1.5
# PDF reports: matplotlib renders the charts, reportlab lays out the PDF.
matplotlib==3.9.2
reportlab==4.2.2
# IANA timezone database so zoneinfo works on slim images / Windows
# (needed to localize Ecowitt export timestamps correctly).
tzdata==2024.1
